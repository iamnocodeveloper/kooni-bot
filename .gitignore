node_modules/
.wrangler/
.dev.vars
.dev.vars.local
*.log
.DS_Store
dist/
.playwright-cli/
.bot-setup.json
.bot-state.json
.bot-version
.env*
!.env.example

# build artifacts
# NOTE: scripts/kb-fixtures.json is imported by the Worker at build time
# (src/kb/reindex.ts) so it MUST be tracked. Default content is `[]`;
# regenerate from member/kb/ with `pnpm kb:reindex`.
docs/_recon-digest.json
.mc-credentials.local
.dashboard-credentials.local

# Sitio web (landing de Kooni) — SOLO USO INTERNO, nunca se sube ni se publica
sitio-web/

# Admin de pagos / licencias — PRIVADO del dueño, nunca se sube a GitHub
admin-pagos/
scripts/gen-license.ts

# wrangler.toml real (contiene IDs de tu cuenta Cloudflare). El template público
# es wrangler.toml.example; tu instalador genera el tuyo.
wrangler.toml

# Backend InsForge (sistema de licencias/instalaciones) — PRIVADO, no va al repo
# público de Kooni. Vive en admin-pagos/ + el proyecto InsForge.
AGENTS.md
insforge.toml
migrations/

# Landing web (va a su propio repo web-kooni-bot)
web/

# InsForge & AI agent skills
.insforge
.agent
.agents
.augment
.claude
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf
